[providers]
node = "..."

[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm run --filter @fusion-bridge/web build"]

[start]
cmd = "pnpm run --filter @fusion-bridge/web start" 