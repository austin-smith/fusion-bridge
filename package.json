{
  "name": "fusion-bridge-monorepo",
  "version": "0.1.0", 
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "npm run build --workspace=@fusion-bridge/web",
    "start": "npm run start --workspace=@fusion-bridge/web"
  }
} 